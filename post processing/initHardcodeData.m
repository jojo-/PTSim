function initHardcodeData()

global rawBusA;

iStop = 0;

iStop = iStop+1;
rawBusA.stop(iStop).name = 'Northfields Ave';
rawBusA.stop(iStop).dist = 0;

iStop = iStop+1;
rawBusA.stop(iStop).name = 'Wollongong TAFE';
rawBusA.stop(iStop).dist = 1100;

iStop = iStop+1;
rawBusA.stop(iStop).name = 'Foley St';
rawBusA.stop(iStop).dist = 1800;

iStop = iStop+1;
rawBusA.stop(iStop).name = 'Greenacre Rd, Blight St';
rawBusA.stop(iStop).dist = 2450;

iStop = iStop+1;
rawBusA.stop(iStop).name = 'Wollongong Hospital';
rawBusA.stop(iStop).dist = 3300;

iStop = iStop+1;
rawBusA.stop(iStop).name = 'Wollongong Station';
rawBusA.stop(iStop).dist = 3750;

iStop = iStop+1;
rawBusA.stop(iStop).name = 'Keira St, Crown St';
rawBusA.stop(iStop).dist = 4020;

iStop = iStop+1;
rawBusA.stop(iStop).name = 'Burelli St';
rawBusA.stop(iStop).dist = 4470;

iStop = iStop+1;
rawBusA.stop(iStop).name = 'Win Stadium';
rawBusA.stop(iStop).dist = 4750;

iStop = iStop+1;
rawBusA.stop(iStop).name = 'Corrimal St, Smith St';
rawBusA.stop(iStop).dist = 5250;

iStop = iStop+1;
rawBusA.stop(iStop).name = 'Wollongong Harbour';
rawBusA.stop(iStop).dist = 5850;

iStop = iStop+1;
rawBusA.stop(iStop).name = 'Gipps St, Georges Pl';
rawBusA.stop(iStop).dist = 6450;

iStop = iStop+1;
rawBusA.stop(iStop).name = 'Bourke St';
rawBusA.stop(iStop).dist = 6800;


iStop = iStop+1;
rawBusA.stop(iStop).name = 'Innovation Campus';
rawBusA.stop(iStop).dist = 8400;

iStop = iStop+1;
rawBusA.stop(iStop).name = 'Cnr Elliotts Rd, Squires Way';
rawBusA.stop(iStop).dist = 8950;

iStop = iStop+1;
rawBusA.stop(iStop).name = 'Princes Hwy, Elliotts Rd';
rawBusA.stop(iStop).dist = 9650;

iStop = iStop+1;
rawBusA.stop(iStop).name = 'Princes Hwy, Feary Meadow';
rawBusA.stop(iStop).dist = 10500;

iStop = iStop+1;
rawBusA.stop(iStop).name = 'Wollongong & Keira High Schools';
rawBusA.stop(iStop).dist = 11100;

iStop = iStop+1;
rawBusA.stop(iStop).name = 'Northfields Ave';
rawBusA.stop(iStop).dist = 13950;

%newRawBusA = rawBusA;